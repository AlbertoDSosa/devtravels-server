{
  "name": "devtravels",
  "version": "0.0.1",
  "description": "Aplicación de Viajes",
  "main": "server/index.js",
  "scripts": {
    "test": "export NODE_ENV=test || NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/*.test.js",
    "test-watch": "nodemon --exec yarn test",
    "start": "export NODE_ENV=production || NODE_ENV= production || SET \"NODE_ENV=production\" && node server/",
    "server-dev": "export NODE_ENV=development || NODE_ENV=development || SET \"NODE_ENV=development\" && nodemon --inspect server/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AlbertoDSosa/devtravels-server.git"
  },
  "keywords": [
    "Travel",
    "Trips"
  ],
  "author": "Alberto D.Sosa <albertodsosa@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.5",
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "jsonwebtoken": "^8.5.1",
    "lodash": "^4.17.11",
    "mongoose": "^5.4.22",
    "multer": "^1.4.1",
    "validate": "^4.5.1",
    "validator": "^10.11.0"
  },
  "devDependencies": {
    "casual": "^1.6.0",
    "expect": "^24.8.0",
    "mocha": "^6.1.4",
    "nodemon": "^1.18.10",
    "supertest": "^4.0.2"
  }
}
